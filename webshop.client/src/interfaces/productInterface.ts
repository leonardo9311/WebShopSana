export interface Product {
    productID: number;       
    productCode: string;     
    title: string;           
    description?: string;    
    price: number;           
    stockQuantity: number;
    quantity: number;
}